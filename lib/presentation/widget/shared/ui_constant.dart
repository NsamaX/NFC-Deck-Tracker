class UIConstant {
  static const double   paddingAround   = 20.0;
  static const double   paddingVertical = 120.0;

  static const double   NfcButtonIcon   = 120.0;
  static const double   NfcButtonDot    = 40.0;
  static const Duration NfcTransitionDuration = Duration(milliseconds: 600);

  static const double   historyDrawerWidth = 200.0;
  static const Duration drawerTransitionDuration = Duration(milliseconds: 200);

  static const double   searchBarHeight = 42.0;
  static const Duration searchBarTransitionDuration = Duration(milliseconds: 300);

  static const Duration snackBarTransitionDuration = Duration(seconds: 1);
}
